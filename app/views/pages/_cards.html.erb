<div class="my-4 text-center">
  <div class="row align-items-top">
    <div class="col-12 col-md-4">
      <div class="card-aurora d-flex flex-column justify-content-between align-items-center">
        <!-- 1. Main Variables -->
        <% total_last = (@months.last.total_assets + @months.last.saved_amount).to_f %>
        <% total_first = (@months.first.total_assets + @months.first.saved_amount).to_f %>
        <!-- 2. Title -->
        <div>
          <h3 class="gradient-title">NET WORTH</h3>
        </div>
        <!-- 3. Assets -->
        <div class="mt-2">
          <div>
            <span class="sign asset">¥</span>
            <span class="asset mb-0 pb-0"><%= number_with_delimiter(total_last.to_i) %></span>
            <p class="text-end subtle my-0 me-0 pe-0 py-0" >by <%= @months.last.date.strftime('%b %Y') %></p>
          </div>
        </div>
        <!-- 4. Caret + up/down text -->
        <div class="d-flex justify-content-center align-items-center mt-3">
          <% if total_last > total_first %>
            <% increase = total_last / total_first * 100.0 %>
            <i class="fa-solid fa-caret-up me-2 mt-1 caret"></i>
            <span>Up by <b style="font-weight: 700;"><%= number_with_delimiter(increase.round) %>%</b> since <%= @months.first.date.strftime('%B %Y') %></span>
          <% else %>
            <% decrease = (1.0 - (total_last / total_first)) * 100.0 %>
            <i class="fa-solid fa-caret-down me-2 mb-1 caret"></i>
            <span>Down by <b style="font-weight: 700;"><%= number_with_delimiter(decrease.round) %>%</b> since <%= @months.first.date.strftime('%B %Y') %></span>
          <% end %>
        </div>
      </div>
    </div>
    <div class="col-12 col-md-4">
      <div class="card-aurora d-flex flex-column justify-content-between align-items-center">
        <h3 class="gradient-title mb-0">RETIREMENT</h3>
        <h3 class="asset mb-0">April 2038</h3>
        <p class="pb-0 mb-0" style="color: var(--aurora-navy); font-size: 1.0rem;">Only 12 years and 4 months to go! 🎉</p>
      </div>
    </div>
    <div class="col-12 col-md-4">
      <% has_events = @months.any? { |month| month.events.any? { |e| e.name != "retirement" } } %>
      <div class="<%= has_events ? 'card-aurora' : 'card-aurora-gradient' %> d-flex flex-column" style="height: 231.85px;">
        <h3 class="gradient-title align-items-start">LIFE EVENTS</h3>
        <% if has_events %>
          <% @months.each do |month| %>
            <div class="d-flex flex-column w-100 mt-2" style="overflow: hidden;">
              <div class="scrollable w-100 d-flex justify-content-center">
                <div style="display: grid; grid-template-columns: 24px 1fr 1fr; gap: 0px 0px; align-items: center;">
                  <% month.events.each do |event| %>
                    <% next if event.name == "retirement" %>
                    <% event_icon = case event.name
                      when "promotion" then "fa-briefcase"
                      when "buying a car" then "fa-car-side"
                      when "birth of a child" then "fa-baby"
                      when "inheritance" then "fa-money-bill-transfer"
                      when "marriage" then "fa-heart"
                      when "buying a house" then "fa-house"
                      else "fa-calendar"
                    end %>
                    <i class="fa-solid dashboard-icon <%= event_icon %>"></i>
                    <span style="color: var(--aurora-navy); font-size: 1.25rem;"><b style="font-weight: 700;"><%= event.month.date.strftime('%b %Y') %></b></span>
                    <span style="color: var(--aurora-body); font-size: 1.25rem; text-align: left;"><%= event[:name].capitalize %></span>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="mt-auto text-center">
            <i class="fa-solid fa-circle-plus dashboard-icon mt-2" style="font-size: 5rem;" data-bs-toggle="modal" data-bs-target="#exampleModal"></i>
            <p class="mt-3">Click the + button to add a life event.</p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
